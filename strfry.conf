# Minimal config for relay-agent (CLI scan/delete only)
# db path must match volume mount: ./nostr/*/data -> /app/strfry-db
db = "/app/strfry-db/"

# Don't set nofiles limit — container max (524288) < strfry default (1000000)
relay {
  nofiles = 0
}
